# Public production origin used for canonical URLs, the sitemap, robots.txt,
# Open Graph tags, and JSON-LD. Set this to your deployed domain (no trailing
# slash). Falls back to http://localhost:3000 during local development.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
